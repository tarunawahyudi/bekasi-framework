<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
      body {
        background-color: #333;
        font-family: Arial, 'sans-serif';
        color: #fff;
        text-align: center;
        margin: 0;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      h1 {
        font-size: 3em;
        background: linear-gradient(45deg, #00FF00, #FFFF00);
        -webkit-background-clip: text;
        color: transparent;
        margin: 0;
      }

      .subheader {
        font-size: 1.5em;
        margin-top: 10px;
        max-with: 60vw;
      }
    </style>
  </head>
  <body>
    <h1>Hello Bekasi</h1>
    <p class="subheader">Bekasi Framework is a lightweight web framework built using TypeScript</p>
  </body>
</html>
